{
  "_comment": "prism model template. deepseek is a BUILT-IN pi provider, so this file only pins display metadata via modelOverrides (non-destructive — it does NOT replace the built-in catalogue). Authenticate with `pi auth login` (or `/login deepseek`) or by exporting DEEPSEEK_API_KEY. NEVER put an API key in this file. Primary: deepseek-v4-flash; Judge (Ctrl+P): deepseek-v4-pro (ADR-0057).",
  "providers": {
    "deepseek": {
      "modelOverrides": {
        "deepseek-v4-flash": { "name": "DeepSeek V4 Flash (prism primary)" },
        "deepseek-v4-pro": { "name": "DeepSeek V4 Pro (prism judge)" }
      }
    }
  }
}
