---
description: Draft or create a scoped issue using existing project conventions.
---

Use `ticketing` and `tracker-operator` for $ARGUMENTS. Inspect existing issues to
avoid duplicates. Capture the problem, scope and acceptance evidence. Follow the
requested draft/create authority without a second approval gate.
