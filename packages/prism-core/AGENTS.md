<!-- prism-core AGENTS.md — STUB (Stage 0 scaffold) -->

# AGENTS.md (prism-core, global)

> **Stage 0 stub.** This is the global core instruction set. The full content
> is ported from `~/tmp/prism/AGENTS.md` in **Stage 1 (Task 1.1)**, with the
> stack-specific sections removed (they move to the adapter's `php-web-stack`
> skill) and the de-opencode-ification edits applied. It deploys to
> `~/.pi/agent/AGENTS.md` via `install-global.sh` (Stage 5) so it is loaded
> every session across every trusted project.

Placeholder — see Stage 1.
