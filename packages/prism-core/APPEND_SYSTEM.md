<!-- prism-core APPEND_SYSTEM.md — STUB (Stage 0 scaffold) -->

# APPEND_SYSTEM.md (prism-core, global)

> **Stage 0 stub.** This is the anti-drift bootstrap, the direct replacement
> for the opencode `session-bootstrap.ts` plugin. The full content is ported
> verbatim from `~/tmp/prism/.opencode/docs/session-bootstrap.md` in
> **Stage 1 (Task 1.2)**, with only the de-opencode-ification edits applied.
> pi appends `~/.pi/agent/APPEND_SYSTEM.md` to the system prompt on every
> turn automatically — no extension required.

Placeholder — see Stage 1.
